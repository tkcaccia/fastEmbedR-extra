# Populate these fields only after the reviewed source and result archive are
# immutable. The preparation job refuses empty values.
export FASTEMBEDR_RELEASE_TAG=
export FASTEMBEDR_RELEASE_VERSION=
export FASTEMBEDR_RELEASE_COMMIT=
export FASTEMBEDR_RELEASE_LABEL=
export FASTEMBEDR_RESULT_DOI=
