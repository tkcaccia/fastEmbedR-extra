# Development evidence

This directory contains compact, explicitly labelled development checks that
help explain validation decisions. They are not release-level benchmark
evidence and are excluded from publication speedup summaries.

Replicate-level outputs belong in the immutable external result archive, not
in Git. Release-level compact summaries are stored under `results/aggregate/`.
